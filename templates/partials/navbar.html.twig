<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
  <a class="navbar-brand" href="#">quizzGame</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarColor01">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item">
        <a class="nav-link" href="{{ path('homepage') }}">accueil</a>
      </li>
      <li class="nav-item active">
        <a class="nav-link" href="{{ path('home')}}">jouer une partie!
          <span class="sr-only">(current)</span>
        </a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="{{ path('score_index')}}">meilleurs scores</a>
      </li>

      {# ...  Connexion / Sign up  #}
	  {% if not app.user %}
      <li class="nav-item">
        <a class="nav-link" href="{{ path('app_login') }}">se connecter</a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="{{ path('app_register') }}">s'inscrire</a>
      </li>
      {% elseif (is_granted('ROLE_USER') and (app.user)) %}
      <li class="nav-item">
        <a class="nav-link" href="{{ path('user_show', {'id': app.user.id}) }}">profil de {{app.user.pseudo}}</a>
      </li>
      {% endif %}
      
      {# ...  About Us  #}
      <li class="nav-item">
        <a class="nav-link" href="{{ path('about_us')}}">a propos</a>
      </li>

      {# ...  Logout  #}
      {% if app.user %}
	  <li class="nav-item active">
        <a class="nav-link" href="{{ path('app_logout') }}">se deconnecter
        <span class="sr-only">(current)</span></a>
      </li>
	  {% endif %}
    </ul>
    
  </div>
  
</nav>